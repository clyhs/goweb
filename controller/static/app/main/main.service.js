(function(){
    'use strict';

    angular
        .module('goweb.main')
        .factory('MainService', MainService)

        MainService.$inject = ['$http'];
    function MainService($http) {
        return {
            
        } 
    }


})();
